# .env dosyasını oluşturmak için: cp .env.example .env

# --- GENEL AYARLAR ---
BACKEND_PORT=8000
FRONTEND_PORT=8501
API_URL=http://backend:8000

# --- QDRANT (embedded / dosya tabanlı, ayrı servis gerekmez) ---
QDRANT_COLLECTION=pet_vectors
